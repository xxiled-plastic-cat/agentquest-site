# Website Contract Pin (v1)

This frontend is pinned to world API contract version **v1**.

Current usage:

- `GET /health` for connectivity checks

Future UI features should use versioned endpoints and payloads from the same contract family.
